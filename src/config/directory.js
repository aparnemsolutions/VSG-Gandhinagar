// Configure a separate Google Sheet that contains the Vihar directory.
// The sheet should be shared so the browser can access it, typically "Anyone
// with the link can view" or a publicly published sheet.
// Example sheet URL: https://docs.google.com/spreadsheets/d/FILE_ID/edit

export const DIRECTORY_SHEET_ID = '1eDRJ0mR4Ho7b-q-AVfJKzwhfwRDLYk_gh13AoirMDCY';
export const DIRECTORY_SHEET_NAME = 'Form responses 1';

// If the directory sheet uses a different name field key, add it here.
export const DIRECTORY_NAME_KEYS = ['Name', 'name', 'Full Name'];
